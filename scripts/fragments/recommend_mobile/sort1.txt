    const renderCol = (col: SelectedTag[], idx: number) => (
      <div key={`sort1-col-${idx}`} style={{ display: 'flex', flexDirection: 'column', gap: 8 }}>
        {col.map(t => {
          const r = sort1Ranks.get(t.tagKey);
          return (
            <button
              key={t.tagKey}
              type="button"
              onClick={() => toggleRank(t.tagKey)}
              style={{
                ...(isMobile
                  ? mobileQuizLikeChoiceStyle({
                      display: 'flex',
                      alignItems: 'center',
                      justifyContent: 'flex-start',
                      textAlign: 'left',
                      backgroundColor: r ? '#dbeafe' : '#faf8f5',
                      color: r ? '#1d4ed8' : 'var(--color-text)',
                      border: `2px solid ${r ? '#3b82f6' : '#e5e7eb'}`,
                      borderRadius: 8,
                      cursor: 'pointer',
                    })
                  : {
                      padding: '10px 14px',
                      fontSize: 14,
                      fontWeight: 500,
                      textAlign: 'left',
                      backgroundColor: r ? '#dbeafe' : '#faf8f5',
                      color: r ? '#1d4ed8' : 'var(--color-text)',
                      border: `2px solid ${r ? '#3b82f6' : '#e5e7eb'}`,
                      borderRadius: 8,
                      cursor: 'pointer',
                    }),
              }}
            >
              {t.displayName}
            </button>
          );
        })}
      </div>
    );
    const sort1MobileBelow =
      isMobile && items.length > 0 ? (
        <div style={{ width: '100%', maxWidth: 480, margin: '0 auto', zoom: MOBILE_WB_ZOOM }}>
          <div style={{ display: 'flex', flexDirection: 'column', gap: 8 }}>
            {items.map(t => {
              const r = sort1Ranks.get(t.tagKey);
              return (
                <button
                  key={t.tagKey}
                  type="button"
                  onClick={() => toggleRank(t.tagKey)}
                  style={mobileQuizLikeChoiceStyle({
                    display: 'flex',
                    alignItems: 'center',
                    justifyContent: 'flex-start',
                    textAlign: 'left',
                    backgroundColor: r ? '#dbeafe' : '#faf8f5',
                    color: r ? '#1d4ed8' : 'var(--color-text)',
                    border: `2px solid ${r ? '#3b82f6' : '#e5e7eb'}`,
                    borderRadius: 8,
                    cursor: 'pointer',
                  })}
                >
                  {t.displayName}
                </button>
              );
            })}
          </div>
        </div>
      ) : undefined;
    const sortBtnStyle = { padding: isMobile ? '8px 16px' : '10px 20px', fontSize: isMobile ? 13 : 14, fontWeight: 600, borderRadius: 8, cursor: 'pointer' as const };
    return (
      <>
        <Stage
          characterVariant="usually"
          characterSpeech={<p style={{ margin: 0, fontWeight: 600, color: 'var(--color-text)', fontSize: isMobile ? 24 : 17 }}>{getSortPromptFront(rc)}</p>}
          whiteboardWide={true}
          mobileBelowCanvas={sort1MobileBelow}
          mobileExtendWhiteboard={isMobile && sort1MobileBelow ? false : undefined}
        >
          <div style={{ padding: isMobile ? '0.5rem 0' : '0.55rem 0', maxWidth: '100%', minWidth: 0 }}>
            {!isMobile && (
              <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr 1fr', gap: 12, marginBottom: 10, maxWidth: 600 }}>
                {col1.length > 0 && renderCol(col1, 0)}
                {col2.length > 0 && renderCol(col2, 1)}
                {col3.length > 0 && renderCol(col3, 2)}
              </div>
            )}
            <div style={{ marginBottom: 12, display: 'flex', flexWrap: 'wrap', gap: 8, flexDirection: 'row' }}>
              {Array.from({ length: sort1Max }, (_, i) => (i + 1) as 1 | 2 | 3 | 4 | 5).map(r => {
                const t = ranked.find((_, i) => i + 1 === r);
                return (
                  <span
                    key={r}
                    style={{
                      padding: '6px 12px',
                      minWidth: 80,
                      fontSize: isMobile ? 13 : 13,
                      backgroundColor: t ? '#dbeafe' : 'transparent',
                      color: t ? '#1d4ed8' : 'var(--color-text-muted)',
                      borderRadius: 8,
                      border: `2px solid ${t ? '#3b82f6' : '#e5e7eb'}`,
                    }}
                  >
                    {r}位{t ? `: ${t.displayName}` : ''}
                  </span>
                );
              })}
            </div>
            <div style={{ display: 'flex', flexWrap: 'wrap', gap: 10, alignItems: 'center', justifyContent: 'space-between', width: '100%' }}>
              <div style={{ display: 'flex', flexWrap: 'wrap', gap: 10, alignItems: 'center' }}>
                <button
                  type="button"
                  onClick={proceedFromSort1}
                  disabled={!canProceedSort1}
                  style={{
                    ...(isMobile
                      ? mobileQuizLikeChoiceStyle({
                          display: 'flex',
                          alignItems: 'center',
                          justifyContent: 'center',
                          fontWeight: 600,
                        })
                      : { padding: '16px 32px', fontSize: 17, fontWeight: 600 }),
                    backgroundColor: canProceedSort1 ? '#3b82f6' : '#e5e7eb',
                    color: canProceedSort1 ? '#fff' : '#9ca3af',
                    border: 'none',
                    borderRadius: 12,
                    cursor: canProceedSort1 ? 'pointer' : 'not-allowed',
                  }}
                >
                  {getBtnNextSortFront(rc)}
                </button>
                <button type="button" onClick={resetSort1} style={{ ...sortBtnStyle, backgroundColor: '#faf8f5', color: 'var(--color-text)', border: '2px solid #e5e7eb' }}>
                  {rc.btnRetry}
                </button>
              </div>
              <button
                type="button"
                onClick={() => {
                  setSort1Ranks(new Map());
                  setFrontNavStack(prev => {
                    if (prev.length === 0) return prev;
                    const ent = prev[prev.length - 1]!;
                    setSelectedFamous(sel => {
                      const popped = sel.slice(-ent.undoCount);
                      setCheckedFamous(new Set(popped.map(x => x.tagKey)));
                      return sel.slice(0, -ent.undoCount);
                    });
                    setStep(ent.returnTo);
                    return prev.slice(0, -1);
                  });
                }}
                style={sortFixBtnStyle}
              >
                <svg style={{ width: 16, height: 16 }} viewBox="0 0 24 24" fill="currentColor">
                  <path d="M7.83 11H20v2H7.83l5.59 5.59L12 20l-8-8 8-8 1.41 1.41L7.83 11z" />
                </svg>
                {rcFixBack}
              </button>
            </div>
          </div>
        </Stage>
