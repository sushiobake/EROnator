# Supabase 用の接続文字列（同期スクリプト専用）
# このファイルをコピーして .env.supabase を作り、値を入れてください。
# .env.supabase は .gitignore 済みなのでコミットされません。

# Pooler（接続プーリング）の URL（Vercel の DATABASE_URL と同じもの）
DATABASE_URL=postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres

# 直接接続の URL（Supabase ダッシュボード → Project Settings → Database）
DIRECT_URL=postgres://postgres.[project-ref]:[password]@db.[project-ref].supabase.co:5432/postgres
